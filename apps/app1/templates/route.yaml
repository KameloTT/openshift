kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: app1-route
  labels:
    app: frontend
  annotations:
    argocd.argoproj.io/sync-wave: '1'
spec:
  to:
    kind: Service
    name: team2-app1-dev-nginx
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None

