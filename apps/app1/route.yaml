kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: app1-route
  labels:
    app: nginx
  annotations:
    argocd.argoproj.io/sync-wave: '1'
spec:
  to:
    kind: Service
    name: nginx-svc
    weight: 100
  port:
    targetPort: 80
  wildcardPolicy: None

